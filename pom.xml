<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.example</groupId>
    <artifactId>SoundPlayerV2</artifactId>
    <version>2.0</version>
    <name>SoundPlayerV2</name>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <jfoenix.version>9.0.10</jfoenix.version>
        <ikonli.version>12.3.1</ikonli.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <javafx.version>21</javafx.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-fxml</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-media</artifactId>
            <version>${javafx.version}</version>
        </dependency>
        <dependency>
            <groupId>com.jfoenix</groupId>
            <artifactId>jfoenix</artifactId>
            <version>${jfoenix.version}</version>
        </dependency>
        <dependency>
            <groupId>org.kordamp.ikonli</groupId>
            <artifactId>ikonli-javafx</artifactId>
            <version>${ikonli.version}</version>
        </dependency>
        <dependency>
            <groupId>org.kordamp.ikonli</groupId>
            <artifactId>ikonli-fontawesome5-pack</artifactId>
            <version>${ikonli.version}</version>
        </dependency>
        <dependency>
            <groupId>org.kordamp.ikonli</groupId>
            <artifactId>ikonli-materialdesign2-pack</artifactId>
            <version>${ikonli.version}</version>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.13.0</version>
                <configuration>
                    <source>21</source>
                    <target>21</target>
                </configuration>
            </plugin>

            <!-- Copy dependencies -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.6.1</version>
                <executions>
                    <execution>
                        <id>copy-dependencies</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>copy-dependencies</goal>
                        </goals>
                        <configuration>
                            <outputDirectory>${project.build.directory}/app/lib</outputDirectory>
                            <includeScope>runtime</includeScope>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Create the main JAR -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.3.0</version>
                <configuration>
                    <outputDirectory>${project.build.directory}/app</outputDirectory>
                    <archive>
                        <manifest>
                            <addClasspath>true</addClasspath>
                            <classpathPrefix>lib/</classpathPrefix>
                            <mainClass>com.example.soundplayerv1.SoundPlayerApplication</mainClass>
                        </manifest>
                    </archive>
                </configuration>
            </plugin>

            <!-- jpackage with icon and version 2.0 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>jpackage</id>
                        <phase>package</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <exec executable="${java.home}/bin/jpackage" failonerror="true">
                                    <arg value="--type"/>
                                    <arg value="exe"/>
                                    <arg value="--name"/>
                                    <arg value="SoundPlayerV2"/>
                                    <arg value="--app-version"/>
                                    <arg value="${project.version}"/>
                                    <arg value="--dest"/>
                                    <arg value="${project.build.directory}/installer"/>

                                    <!-- Use module mode (CRITICAL FIX) -->
                                    <arg value="--module-path"/>
                                    <arg value="${project.build.directory}/app/lib;${project.build.directory}/app"/>
                                    <arg value="--module"/>
                                    <arg value="com.example.soundplayerv1/com.example.soundplayerv1.SoundPlayerApplication"/>

                                    <!-- JVM Options -->
                                    <arg value="--java-options"/>
                                    <arg value="--add-opens=javafx.controls/com.sun.javafx.scene.control.behavior=com.jfoenix"/>
                                    <arg value="--java-options"/>
                                    <arg value="--add-opens=javafx.graphics/com.sun.javafx.scene=com.jfoenix"/>
                                    <arg value="--java-options"/>
                                    <arg value="--add-opens=javafx.base/com.sun.javafx.runtime=ALL-UNNAMED"/>

                                    <!-- Windows specific -->
                                    <arg value="--win-shortcut"/>
                                    <arg value="--win-menu"/>
                                    <arg value="--win-dir-chooser"/>

                                    <!-- Icon -->
                                    <arg value="--icon"/>
                                    <arg value="C:\Users\user\IdeaProjects\SoundPlayerV2\SoundPlayerV2.ico"/>
                                </exec>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>